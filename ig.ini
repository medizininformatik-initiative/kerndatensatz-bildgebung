[IG]
; MII KDS module IG build configuration.
;
; [IG] MUST be the very first line of this file: the IG Publisher's
; checkNewIg() rejects an ig.ini unless its content starts with "[IG]"
; (s.startsWith("[IG]")), so any leading comment would make the build fail with
; "unable to find an ig.ini". Keep [IG] on line 1; put all comments below it
; (this mirrors the ig-template-mii-kds preview ig.ini).
;
; ig       = the SUSHI-generated ImplementationGuide resource; its file name
;            derives from `id` in sushi-config.yaml (replace bildgebung
;            there and here).
; template = the IG template. The IG Publisher reads this as a package
;            reference (id#version) UNLESS it starts with `#`, which makes it a
;            LOCAL template folder relative to this IG root. `#ig-template`
;            therefore loads the vendored MII template from ig-template/
;            (ig-template/package/package.json is type fhir.template, plus
;            ig-template/includes/ and ig-template/content/). This is the
;            documented bring-up fallback: the published package
;            de.medizininformatikinitiative.template is not published to a
;            FHIR package registry yet (GitHub releases of ig-template-mii-kds
;            do exist). Once it is consumable as a package, switch this line to
;            `template = de.medizininformatikinitiative.template#<version>` and
;            delete ig-template/ — see docs/recipes/switch-template-to-published.md
;            and ig-template/README.md.
ig = fsh-generated/resources/ImplementationGuide-mii-ig-bildgebung.json
template = #ig-template

; Those two keys are the whole file. Everything else about the IG — including
; template parameters such as copyrightyear or releaselabel — is configured in
; the ImplementationGuide resource, which SUSHI generates from
; sushi-config.yaml; see its `parameters:` block.
;
; One house rule the convention check enforces: never float a version. A
; `template =` (or a dependency) pinned to #current / #latest / #dev / #cibuild
; fails `node scripts/convention-check.mjs`, because a rebuild years from now
; must produce the same site.
;
; Reference: https://build.fhir.org/ig/FHIR/ig-guidance/
;            https://confluence.hl7.org/display/FHIR/Implementation+Guide+Parameters
